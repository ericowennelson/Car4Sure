<form style="display:inline-block" method="post" action="{{ path('app_holder_delete', {'id': holder.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ holder.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
