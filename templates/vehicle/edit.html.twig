{% extends 'base.html.twig' %}

{% block title %}Edit Vehicle{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Vehicle</h2>
        <div class="card">
            <div class="card-header">

        <a class="btn btn-sm btn-outline-primary float-right" href="{{ path('app_vehicle_index') }}"> 
                    Vehicle list
                </a>

    </div>
            <div class="card-body">
                {% include 'vehicle/_form.html.twig' with {'button_label': 'Save', 'button_color': 'btn-success'}  %}
            </div>
            
        </div>
    </div>
{% endblock %}
